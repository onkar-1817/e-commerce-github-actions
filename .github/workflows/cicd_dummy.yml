# Goal: To build a CI/CD pipeline (just dummy)

 name: CI_CD_Dummy

 on:
   push:

 jobs:
   Code:
     runs-on: ubuntu-latest
     steps:
       - name: Clone the code
         uses: actions/checkout@v7

   Build:
     needs: Code
     runs-on: ubuntu-latest
     steps:
       - name: build the coode with docker 
         uses: docker/setup-buildx-action@v3

   Test:
     needs: Build
     runs-on: ubuntu-latest
     steps:
       - name: Test the code
         run: echo "test the code"
         
   Deploy:
     needs: [Code,Build,Test]
     runs-on: ubuntu-latest
     steps:
       - name: Run the application
         run: echo "Deploy live server"


